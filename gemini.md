# General Instructions

- **Firestore Rules**: Before updating `firestore.rules`, ALWAYS fetch the latest version from the Firebase project to ensure no changes (e.g., made via the Console) are lost.
